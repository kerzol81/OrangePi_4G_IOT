# Install Ubuntu 18.04 in VMWare

# these might need if building fails

# https://packages.ubuntu.com/search?keywords=lib32stdc%2B%2B6
apt-get install lib32stdc++6

#
apt-get install fakeroot
apt-get install debootstrap
apt-get install schroot
apt-get install binfmt-support qemu qemu-user-static

# change REPOSITORY Server from default to Official Repository Server

# GREEN LED - proper boot

# The firmware that comes on the board supports HDMI 720 x1280 at 48Hz, most monitorst dont wake up under 60Hz
